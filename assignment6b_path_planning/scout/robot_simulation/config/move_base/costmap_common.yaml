footprint: [[-0.35, -0.35], [-0.35, 0.35], [0.35, 0.35], [0.35, -0.35]]
footprint_padding: 0.02

robot_base_frame: dummy_base_link
transform_tolerance: 0.5

resolution: 0.05


# obstacle_range: 5.5
# raytrace_range: 6.0

#layer definitions
static:
    map_topic: /map
    subscribe_to_updates: false

# obstacles_laser:
#     observation_sources: laser
#     laser: {data_type: LaserScan, clearing: true, marking: true, topic: /scan_2d, inf_is_valid: true}

obstacles_laser:
  observation_sources: lidar_points
  lidar_points: {data_type: PointCloud2, clearing: true, marking: true, topic: /velodyne_points, inf_is_valid: false, max_obstacle_height: 1, min_obstacle_height: 0.15, sensor_frame: velodyne}
    
    
    

    
    
    # obstacle_range: 2.5
    # raytrace_range: 3.5
    

inflation:
    inflation_radius: 0.1
